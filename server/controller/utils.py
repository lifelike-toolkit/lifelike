"""Utilities classes for controllers"""
from character import Character
import json

def getCharacter(name: str) -> Character:
    pass

def serializeCharacter(character: Character) -> str:
    """
    Possibly temporary for v1
    Saves to file with according name in currentCharacters
    Since characters suffer from dementia, we can stay central here
    """
    pass